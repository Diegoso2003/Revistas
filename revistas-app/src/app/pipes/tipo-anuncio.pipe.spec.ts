import { TipoAnuncioPipe } from './tipo-anuncio.pipe';

describe('TipoAnuncioPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoAnuncioPipe();
    expect(pipe).toBeTruthy();
  });
});
